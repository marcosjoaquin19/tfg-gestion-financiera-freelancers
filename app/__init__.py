"""
Paquete principal de la aplicación (backend).

Agrupa todo el código del servidor: la configuración (main, database,
dependencies) y los subpaquetes models (tablas), schemas (validación), routers
(endpoints HTTP) y services (lógica de negocio). Su presencia convierte a `app`
en un paquete Python importable (ej: `from app.main import app`).
"""
